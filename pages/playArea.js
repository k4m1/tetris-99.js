class playArea {
    constructor()
    {
        const matrix = [];
        while (h--) {
            // while height is not 0
            matrix.push(new Array(w)).fill(0)
        }
        this.matrix = matrix;

    }

}

